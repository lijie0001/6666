FROM python:3.12-slim

WORKDIR /app

# 只安装网页需要的依赖，加快构建
RUN pip install --no-cache-dir flask requests beautifulsoup4 python-dotenv

COPY app.py crawler.py image_crawler.py .
COPY templates/ templates/

# Railway 注入 PORT，必须监听 0.0.0.0
ENV PORT=5000
EXPOSE 5000

CMD ["python", "app.py"]
